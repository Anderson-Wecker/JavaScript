let nome = "Anderson";

if(nome == "Anderson")
{
    console.log(`Seu nome é ${nome}`);
}
    else 
    {
        console.log(`Seu nome não é ${nome}`);
    }


let velocidade = 70;

if(velocidade <= 80)
{
    console.log('Não foi multado.');
}
    else
    {
        console.log('Foi multado !')
    }

var nota1;
var nota2;

nota1 = prompt("Qual a sua nota ?");
nota2 = prompt("Qual a sua nota ?");

var soma = nota1 + nota2;

console.log(`Sua nota é ${soma}`);
